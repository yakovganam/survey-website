# Environment example for Survey Site
# Copy to .env and adjust values for your server

# Port your app listens on inside the container / PM2
PORT=3000

# MongoDB connection string
# If using local Docker Mongo, example:
# MONGODB_URI=mongodb://mongo:27017/surveys
# If using external/local host Mongo, example:
# MONGODB_URI=mongodb://localhost:27017/surveys
MONGODB_URI=mongodb://localhost:27017/surveys

# Allowed CORS origin(s) for the frontend
# Single origin example:
ALLOWED_ORIGIN=http://localhost:3000
# Multiple origins (if your code supports comma-separated):
# ALLOWED_ORIGIN=http://localhost:3000,https://your-domain.com
